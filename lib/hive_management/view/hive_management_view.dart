import 'package:flutter/material.dart';

class HiveManagementView extends StatelessWidget {
  const HiveManagementView({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
